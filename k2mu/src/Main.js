// imports
import React from "react";

export default class Main extends React.Component {
    // state
    state = {
        currentPage: "home"
    };

    render() {
        return (
            <React.Fragment>
                <h1>hi.</h1>
            </React.Fragment>
        )
    }
}